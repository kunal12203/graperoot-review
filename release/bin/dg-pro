#!/usr/bin/env bash
# dg-pro — GrapeRoot Pro launcher for OpenAI Codex
set -euo pipefail
INSTALL_DIR="${GRAPEROOT_PRO_HOME:-$HOME/.graperoot-pro}"
exec "$INSTALL_DIR/venv/bin/python3" "$INSTALL_DIR/launch.py" "${1:-.}" --codex "${@:2}"
